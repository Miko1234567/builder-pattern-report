# Builder Pattern Assignment - Report System

## Overview
This project implements the **Builder Creational Design Pattern** in Java. It constructs complex `Report` objects (PDF and HTML formats) step-by-step using a fluent API and a Director class.

## How to Run
1. Open the project in IntelliJ IDEA.
2. Run `Main.java`.
